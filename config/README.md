# Config profiles

Real YAML profiles for the OpenWiki TUI harness and the NPU concurrency policy.

| File | What |
|---|---|
| `file_administrator.yaml` | File-admin profile (GLM-5.2 backed) |
| `oeracle_helpdesk.yaml` | Œræcle help-desk profile (on-device 9B) |
| `concurrency.yaml` | `DISALLOW_NPU_OVERLAP` and related policy |

**Status:** the folder exists, the YAMLs are not yet transcribed from source
(S7/S8 ARCHITECTURE BLUEPRINT Pt.1 & Pt.2, ~2 KB each). Tracked as an open item —
do not mark this done until the real files land.
