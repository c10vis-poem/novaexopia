# Moonshine STT Engine

**Slug:** `moonshine-onnx-engine` · **Kind:** engine (runtime + model + scheduler)

**Built on runtime:** `onnx-runtime`

## Notes

STT. ~10s HARD INPUT CEILING — silent failure past 9.5-10s, no error raised. Chunk before this limit.
