# whisper.cpp

**Slug:** `whisper-cpp` · **Kind:** runtime (executes code)

## What it is

whisper-server, whisper-quantize

**Target hardware:** CPU (glibc — needs Debian proot)

## Notes

At ~/tools/whisper-parakeet with NVIDIA Parakeet CLI/test binaries alongside.

## Do not confuse with

An *engine* — an engine wraps this plus a model plus a scheduler. This is the
layer that actually executes.
