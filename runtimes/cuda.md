# CUDA

**Slug:** `cuda` · **Kind:** runtime (executes code)

## What it is

CUDA runtime + toolchain

**Target hardware:** Jetson Orin Nano (Node Beta)

## Notes

Not procured yet. Deploy scripts already written at aesop-xi/deploy/jetson/.

## Do not confuse with

An *engine* — an engine wraps this plus a model plus a scheduler. This is the
layer that actually executes.
